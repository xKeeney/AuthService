# Future features
- [ ] Audit log (login / refresh / revoke)
- [ ] Roles + permissions (admin / user / support / service)

# v0.1.0
- [ ] JWT tokens logic (RS256 algorithm, refresh tokens can be used only one time)
- [ ] **Register** endpoint
- [ ] **Login** endpoint
- [ ] **Refresh** endpoint
- [ ] **Logout** endpoint
- [ ] Secutity event if refresh with already revoked token